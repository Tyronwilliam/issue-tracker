import React from "react";

const InvitationLayout = ({ children }: { children: React.ReactNode }) => {
  return <main className="p-5 w-full h-screen	">{children}</main>;
};

export default InvitationLayout;
